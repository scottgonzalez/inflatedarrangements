<ul class='category-listing'>
<% categories.each do |category| %>
  <li>
    <%= link_to category.name, category, class: 'category' %>
    <%= pluralize(category.product_count, 'product') %>
  </li>
<% end %>
</ul>
